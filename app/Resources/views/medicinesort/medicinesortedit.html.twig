{% extends "base.html.twig" %}

{% block content %}
    <div class="container">
        <form action="" method="post">
            药品类别ID：<div class="form-group">
                {{ form_widget(form.sortId,{ 'attr':{'class':'form-control'}}) }}
            </div>
            药品类别名称：<div class="form-group">
                {{ form_widget(form.sortName,{  'attr':{'class': (label_attr.class|default('form-control') ~ ' required')|trim }}) }}
            </div><div class="form-group">
                {#{{ form_widget(form.save,{ 'label':' 保 存 ','attr':{'class':'btn btn-success'}}) }}#}
                <input type="submit" class="btn btn-success" value="Sauvgarder">
            </div>
            <div>
                {{ form_end(form) }}
            </div>
        </form>
    </div>
{% endblock %}
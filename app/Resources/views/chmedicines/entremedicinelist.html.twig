{% extends "base.html.twig" %}

{% block main %}
    <table class="table table-hover table-responsive">
        {% for meentre in meentrepris %}
        <thead>
        <tr>
            <th colspan="3" style="text-align: center;">{{ meentre.name }}</th>
        </tr>
        <tr>
            <th>药品名称</th>
            <th>剂型</th>
            <th>批准文号</th>
            <th>操  作</th>
        </tr>
        </thead>
        <tbody>
        {%  for ch in  meentre.chmedicine %}
            <tr>
                <td>{{ ch.breed }}</td>
                <td>{{ ch.Dosage_form }}</td>
                <td>{{ ch.Approval_number }}</td>
                <td><a href="{{ path('chmedicine_detail',{id:ch.id}) }}" class="btn btn-sm btn-default">
                        <i class="fa fa-eye" aria-hidden="true"></i> {{ 'action.show'|trans }}
                    </a>
                    <a href="" class="btn btn-default btn-sm">编辑</a>
                    <a href="" class="btn btn-default btn-sm">删除</a>
                </td>
            </tr>
        {% endfor %}
        {#<tr>
            <td>{{ meentre.id }}</td>
            <td>{{ meentre.name }}</td>
            <td><a href="" class="btn btn-default btn-sm">编辑</a>
                <a href="" class="btn btn-default btn-sm">删除</a>
            </td>
        </tr>#}
        </tbody>

        {% endfor %}
    </table>
{% endblock %}